apiVersion: 1

datasources:
  - name: Prometheus
    # Stable uid so provisioned dashboards can reference this datasource.
    uid: prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
