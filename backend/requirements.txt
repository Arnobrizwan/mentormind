# MentorMind backend — latest versions, resolved at install time.
# Pin with `pip freeze > requirements.lock` before production deploys.
Django
djangorestframework
djangorestframework-simplejwt
drf-spectacular
django-cors-headers
django-environ
django-redis
django-prometheus
django-filter
celery[redis]
psycopg[binary]
gunicorn
whitenoise
# Phase 3: websockets chat, object-storage uploads, image fields
channels
channels-redis
daphne
django-storages[s3]
Pillow
uvicorn[standard]
uvicorn-worker
# PWA web-push reminders (optional feature; inert unless VAPID keys are set)
pywebpush
