# Phase 4: classic CV workloads (no GPU needed). PyTorch deliberately
# omitted — Haar cascades + grid OMR + Tesseract cover the demo features
# at a fraction of the image size.
# Pinned (==) for reproducible images; bump deliberately, not on rebuild.
fastapi==0.136.3
uvicorn[standard]==0.49.0
# .env loading at startup (app/main.py) — also a uvicorn[standard] extra,
# but pinned here because the app imports it directly.
python-dotenv==1.2.1
opencv-python-headless==4.13.0.92
numpy==2.4.6
pillow==12.2.0
python-multipart==0.0.32
pytesseract==0.3.13
# Past-paper training pipeline (Cambridge OCR -> aligned dataset)
mistralai==2.4.9
sqlalchemy[asyncio]==2.0.50
aiosqlite==0.22.1
greenlet==3.5.1
# Async HTTP for flags polling + custom-LLM calls (no event-loop blocking)
httpx==0.28.1
# /metrics for the Prometheus scrape job
prometheus-fastapi-instrumentator==8.0.0
sentence-transformers

